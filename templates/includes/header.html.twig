<header id="header">
    <nav class="navbar navbar-fixed-top" role="banner">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="{{ path('home') }}">Phil's Ads</a>
            </div>

            <div class="collapse navbar-collapse navbar-right">
                <ul class="nav navbar-nav">
                    <li class="active"><a href="{{ path('home') }}">Accueil</a></li>
                    <!--<li><a href="about-us.html">A propos</a></li>-->
                    <li><a href="{{ path('adverts') }}">Annonces</a></li>
                    <!--<li><a href="portfolio.html">Portfolio</a></li>-->
                    <!--<li><a href="blog.html">Blog</a></li>-->
                    <li><a href="{{ path('contact') }}">Contact</a></li>
                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        {% if is_granted('ROLE_ADMIN') %}
                            <li class="dropdown">
                                <button class="btn btn-default dropdown-toggle" type="button" data-toggle="dropdown">Admin
                                    <span class="caret"></span></button>
                                <ul class="dropdown-menu">
                                    <li><a href="{{ path('admin_users') }}">Gestion Utilisateurs</a></li>
                                    <li><a href="{{ path('admin_adverts') }}">Gestion annonces</a></li>
                                    <li><a href="#">Gestion candidatures</a></li>
                                    <li><a href="{{ path('admin_categories') }}">Gestion catégories</a></li>
                                </ul>
                            </li>
                        {% endif %}
                    {% endif %}

                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        <li class="dropdown">
                            <button class="btn btn-default dropdown-toggle" type="button" data-toggle="dropdown">Mon compte
                                <span class="caret"></span></button>
                            <ul class="dropdown-menu">
                                <li><a href="{{ path('account', {id: app.user.id}) }}">Mon profil</a></li>
                                <li><a href="{{ path('my_adverts', {id: app.user.id}) }}">Mes annonces</a></li>
                                <li><a href="{{ path('add_advert') }}">Ajouter une annonce</a></li>
                                <li><a href="{{ path('add_cv') }}">Télécharger mon CV</a></li>
                                <li><a href="{{ path('security_logout') }}">Déconnexion</a></li>
                            </ul>
                        </li>


                    {% else %}
                        <li><a href="{{ path('security_register') }}">Inscription</a></li>
                        <li><a href="{{ path('security_login') }}">Connexion</a></li>
                    {% endif %}
                </ul>
            </div>
        </div><!--/.container-->
    </nav><!--/nav-->

</header><!--/header-->